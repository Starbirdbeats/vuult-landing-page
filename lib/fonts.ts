import { Antic_Slab, Alex_Brush, Cormorant } from 'next/font/google';

export const anticSlab = Antic_Slab({ weight: '400', subsets: ['latin'] });
export const alexBrush = Alex_Brush({ weight: '400', subsets: ['latin'] });
export const cormorant = Cormorant({ subsets: ['latin'] });
